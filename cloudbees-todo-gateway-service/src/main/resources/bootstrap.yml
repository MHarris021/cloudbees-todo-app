spring:
  cloud:
    config:
      name: todo-gateway-service
      discovery:
        service-id: todo-config-service
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/

server:
  port: 8761

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false

spring:
  application:
    name: todo-discover-service
  cloud:
    config:
      name: todo-discovery-service
      uri: http://localhost:8888
